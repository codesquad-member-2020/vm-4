const VENDINGMACHINE = "/vendingmachine";
const ITEMS = "/items";
const WALLET = "/wallet";

const routes = {
  vendingmachine: VENDINGMACHINE,
  items: ITEMS,
  wallet: WALLET
};

module.exports = routes;
